import React from 'react';
import Addition from './Addition'

const Header = (props) =>
{
    return(
        <header>
            <div className='container'>
                <h1>SUBSCLIFE</h1>
            </div>
        </header>
    );
};

export default Header;